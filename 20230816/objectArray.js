/*
 * Objeto es un conjunto de propiedades (clave:valor)
 */

const jugador = { name: "Maria", age: 20, estado: true, saludar: function () { } }

/*
 * Array conjunto de elementos (posición)
 */

const datos = ["Colombia", 20, true, [10, 20], null]